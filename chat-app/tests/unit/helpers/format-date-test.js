import {
  formatDate
} from 'chat-app/helpers/format-date';

module('FormatDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDate(42);
  ok(result);
});
